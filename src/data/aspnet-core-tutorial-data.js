export const aspnetCoreTutorialQuestions = [
  {
    "id": 1,
    "title": "1. Installation of .NET Core and ASP.NET Core",
    "category": "Phase 1: Installation & Architecture",
    "language": "bash",
    "explanation": "<h3>The Foundation of .NET Development</h3>\n<p>To begin building high-performance web applications with ASP.NET Core, the absolute first step is setting up your development environment. Unlike older versions of the .NET Framework which were tightly coupled to Windows, modern .NET Core (now just called .NET) is completely cross-platform.</p>\n<h4>What exactly are you installing?</h4>\n<p>When you download .NET, you are typically downloading the <strong>.NET SDK (Software Development Kit)</strong>. The SDK is a massive bundle that includes:</p>\n<ul>\n    <li><strong>The .NET Runtime (CLR):</strong> The engine that actually runs your compiled code. It manages memory (Garbage Collection), thread execution, and provides the base types (like strings and integers).</li>\n    <li><strong>The ASP.NET Core Runtime:</strong> Specialized libraries specifically for handling web traffic, HTTP requests, routing, and HTML generation.</li>\n    <li><strong>The .NET CLI:</strong> The Command-Line Interface tool (<code>dotnet</code>) which you use to create, build, run, and publish applications without needing a heavy IDE.</li>\n    <li><strong>Compilers:</strong> The Roslyn compiler that turns your C# code into Intermediate Language (IL).</li>\n</ul>\n<h4>Choosing an IDE (Integrated Development Environment)</h4>\n<p>While the CLI allows you to code in Notepad if you wanted to, professional developers use specialized IDEs:</p>\n<ul>\n    <li><strong>Visual Studio:</strong> The industry standard for Windows developers. It is extremely powerful, featuring advanced debugging, memory profiling, and built-in database management.</li>\n    <li><strong>Visual Studio Code:</strong> A lightweight, cross-platform code editor heavily favored by full-stack developers. It relies on extensions (like the C# Dev Kit) to provide intelligent code completion.</li>\n    <li><strong>JetBrains Rider:</strong> A highly popular cross-platform IDE known for its extreme speed and advanced refactoring tools.</li>\n</ul>",
    "code": "# 1. Verify your installation by checking the SDK version\ndotnet --version\n\n# 2. View a list of all SDKs installed on your machine\ndotnet --list-sdks\n\n# 3. View a list of all Runtimes installed\ndotnet --list-runtimes",
    "tip": "When setting up a production server to host your completed app, you do NOT install the SDK. You only install the ASP.NET Core Hosting Bundle (Runtime). The SDK contains compilers and developer tools that take up space and present security risks on a public server.",
    "summary": "Installing the .NET SDK provides the runtime engine, web libraries, and command-line tools necessary to build and run ASP.NET Core applications across any operating system."
  },
  {
    "id": 2,
    "title": "2. ASP.NET Core Application Architecture",
    "category": "Phase 1: Installation & Architecture",
    "language": "csharp",
    "explanation": "<h3>The Secret: It's Just a Console App!</h3>\n<p>One of the most mind-blowing revelations for new developers is that <strong>an ASP.NET Core web application is, at its core, just a simple Console Application.</strong></p>\n<p>In older versions of ASP.NET, web applications were deeply tied to IIS (Internet Information Services), the Windows web server. The application couldn't even start without IIS spinning it up. ASP.NET Core completely revolutionized this by making the application self-hosting.</p>\n<h4>How does a Console App become a Web Server?</h4>\n<p>When you run your ASP.NET Core application, the <code>Program.Main()</code> method executes (just like in a console app). Inside this method, the framework does something special: it builds and launches a high-performance, built-in web server called <strong>Kestrel</strong>.</p>\n<p>Kestrel sits inside your console application, binds to a port (like 5000), and begins listening for raw HTTP traffic from the internet. When a request arrives, Kestrel translates the raw HTTP bytes into a C# object called an <code>HttpContext</code>, and hands it off to your application's middleware pipeline.</p>\n<h4>Why is this better?</h4>\n<ul>\n    <li><strong>Speed:</strong> Kestrel is insanely fast, consistently ranking at the top of the TechEmpower Web Framework Benchmarks.</li>\n    <li><strong>Portability:</strong> Because the app brings its own web server, you can run it identically on a Windows machine, a Linux Docker container, or a Raspberry Pi.</li>\n</ul>",
    "diagram": "flowchart TD\n    OS[\"Operating System (Windows/Linux)\"] -->|\"Starts Process\"| Console[\"Console Application (Program.cs)\"]\n    Console -->|\"Configures & Launches\"| Kestrel[\"Kestrel Web Server\"]\n    Internet((\"The Internet\")) -->|\"HTTP GET /api/users\"| Kestrel\n    Kestrel -->|\"Translates to HttpContext\"| Pipeline[\"ASP.NET Core Middleware Pipeline\"]\n    \n    style OS fill:#3b82f6,color:#fff\n    style Console fill:#8b5cf6,color:#fff\n    style Kestrel fill:#f59e0b,color:#fff\n    style Pipeline fill:#10b981,color:#fff",
    "tip": "While Kestrel is incredibly fast, it lacks some advanced edge-server features like rate-limiting and robust SSL termination. In enterprise production environments, Kestrel is usually placed behind a 'Reverse Proxy' like Nginx, Apache, or IIS.",
    "summary": "ASP.NET Core decoupled the framework from IIS, turning web apps into self-hosting console applications powered by the lightning-fast Kestrel web server."
  },
  {
    "id": 3,
    "title": "3. ASP.NET Core - Project Structure",
    "category": "Phase 1: Installation & Architecture",
    "language": "csharp",
    "explanation": "<h3>Anatomy of an ASP.NET Core Project</h3>\n<p>When you create a new ASP.NET Core project using the CLI or Visual Studio, the framework generates a highly opinionated folder structure designed to keep your enterprise applications organized and maintainable.</p>\n<h4>The Critical Components:</h4>\n<ol>\n    <li><strong><code>Program.cs</code></strong>: The absolute entry point of the application. This is where the Kestrel server is built, Dependency Injection services are registered, and the HTTP request pipeline is configured. (In .NET 5 and older, this logic was split across <code>Program.cs</code> and <code>Startup.cs</code>).</li>\n    <li><strong><code>appsettings.json</code></strong>: The central nervous system for configuration. This replaces the old XML-based <code>web.config</code>. It stores JSON-formatted key-value pairs like database connection strings, third-party API keys, and logging levels.</li>\n    <li><strong><code>Properties / launchSettings.json</code></strong>: This file is purely for local development. It tells Visual Studio or the .NET CLI what port to open, what environment variables to set (e.g., <code>ASPNETCORE_ENVIRONMENT=Development</code>), and whether to launch a browser when you hit F5. It is NEVER deployed to production.</li>\n    <li><strong><code>Controllers /</code></strong>: A folder that holds your C# Controller classes. Controllers act as the \"Brain\" of the application, receiving incoming HTTP requests, talking to the database or business logic, and returning responses (HTML or JSON).</li>\n    <li><strong><code>wwwroot /</code></strong>: The only folder directly exposed to the public internet. It holds static assets like CSS, JavaScript, and Images.</li>\n</ol>",
    "diagram": "flowchart LR\n    Root[\"MyStoreApp (Project Root)\"] --> Prop[\"/Properties (launchSettings.json)\"]\n    Root --> Cont[\"/Controllers (Logic)\"]\n    Root --> WWW[\"/wwwroot (Static Files)\"]\n    Root --> AppSet[\"appsettings.json (Config)\"]\n    Root --> Prog[\"Program.cs (Startup)\"]\n    \n    style Root fill:#3b82f6,color:#fff\n    style Prog fill:#10b981,color:#fff\n    style AppSet fill:#f59e0b,color:#fff",
    "mistake": "Do NOT confuse `appsettings.json` with `launchSettings.json`. `appsettings.json` travels with your app to production and controls the application's actual behavior. `launchSettings.json` only affects how the app launches on your local developer machine.",
    "summary": "The project structure separates configuration (appsettings.json), developer launch profiles (launchSettings.json), public assets (wwwroot), and startup logic (Program.cs) into clean, distinct areas."
  },
  {
    "id": 4,
    "title": "4. ASP.NET Core Overview",
    "category": "Phase 1: Installation & Architecture",
    "language": "csharp",
    "explanation": "<h3>Why did Microsoft reinvent ASP.NET?</h3>\n<p>The original ASP.NET framework was massively successful, but after 15 years, it accumulated serious technical debt. It was fundamentally tied to Windows, required massive memory footprints, and forced developers to use IIS.</p>\n<p>Microsoft decided to burn the ships and rewrite the entire framework from scratch. The result was <strong>ASP.NET Core</strong>.</p>\n<h4>The Core Pillars of the Rewrite:</h4>\n<ul>\n    <li><strong>True Cross-Platform:</strong> It had to run natively on Linux and macOS to compete with Node.js and Java in modern cloud/Docker environments.</li>\n    <li><strong>Unified Programming Model:</strong> In the past, MVC (returning HTML) and Web API (returning JSON) were completely separate frameworks with separate base classes. ASP.NET Core unified them into a single, elegant model where a Controller can seamlessly return a View or JSON data.</li>\n    <li><strong>First-Class Dependency Injection (DI):</strong> DI was built directly into the core of the framework, forcing developers to write modular, testable code without relying on bulky third-party libraries like Ninject or Autofac.</li>\n    <li><strong>Modular Pipeline:</strong> You only include the features you need. If you don't need authentication, you don't add the middleware. This keeps the application incredibly lightweight and fast.</li>\n    <li><strong>Open Source:</strong> The entire source code is available on GitHub, allowing the global community to contribute performance optimizations.</li>\n</ul>",
    "summary": "ASP.NET Core is a complete architectural rewrite designed for blistering speed, cloud-native deployments, cross-platform compatibility, and a unified MVC/API programming model."
  },
  {
    "id": 5,
    "title": "5. ASP.NET Core - wwwroot Folder",
    "category": "Phase 2: Project Structure & Configuration",
    "language": "csharp",
    "explanation": "<h3>The Public Face of Your App</h3>\n<p>Security is paramount in web development. You never want a user to accidentally access your raw C# source code files, configuration files, or database scripts by simply typing their path into a browser URL.</p>\n<p>To solve this, ASP.NET Core uses the <strong><code>wwwroot</code></strong> folder. This folder acts as the \"Web Root\"—it is a strict security boundary. <strong>Only files placed inside <code>wwwroot</code> can be directly accessed over the internet.</strong> Everything outside this folder is strictly hidden and protected.</p>\n<h4>What belongs in wwwroot?</h4>\n<ul>\n    <li>Compiled CSS stylesheets (e.g., <code>site.css</code>).</li>\n    <li>Client-side JavaScript files (e.g., <code>app.js</code>, React bundles, Angular bundles).</li>\n    <li>Images, SVGs, and Favicons.</li>\n    <li>Standard HTML files.</li>\n</ul>\n<h4>What does NOT belong in wwwroot?</h4>\n<ul>\n    <li>C# source code (<code>.cs</code>).</li>\n    <li>Razor Views (<code>.cshtml</code>) – these are compiled on the server and should never be downloaded directly by the user.</li>\n    <li>Secrets or configuration files like <code>appsettings.json</code>.</li>\n</ul>",
    "diagram": "flowchart TD\n    Internet((\"Hacker / User\")) -->|\"GET /appsettings.json\"| Root[\"Project Root\"]\n    Root -.->|\"ACCESS DENIED (404)\"| AppSet[\"appsettings.json\"]\n    Root -.->|\"ACCESS DENIED (404)\"| CS[\"Program.cs\"]\n    \n    Internet -->|\"GET /css/style.css\"| WWW[\"/wwwroot\"]\n    WWW -->|\"ACCESS GRANTED (200)\"| CSS[\"/css/style.css\"]\n    \n    style WWW fill:#10b981,color:#fff\n    style Root fill:#ef4444,color:#fff",
    "tip": "Just placing files in `wwwroot` isn't enough. By default, ASP.NET Core is so secure it won't even serve files from `wwwroot`! You must explicitly tell the framework to serve them by adding `app.UseStaticFiles();` inside your `Program.cs`.",
    "summary": "The wwwroot folder forms a critical security boundary. It is the only directory whose static assets are allowed to be served directly to web browsers."
  },
  {
    "id": 6,
    "title": "6. ASP.NET Core - Program.cs",
    "category": "Phase 2: Project Structure & Configuration",
    "language": "csharp",
    "explanation": "<h3>The Control Center</h3>\n<p>In modern .NET (versions 6, 7, and 8+), the entire configuration of your application has been consolidated into a single, remarkably clean file: <strong><code>Program.cs</code></strong>.</p>\n<p>Behind the scenes, it uses a feature called \"Top-Level Statements\" which hides the boilerplate <code>namespace</code>, <code>class Program</code>, and <code>static void Main()</code> syntax, allowing you to write setup code directly at the root of the file.</p>\n<h4>The Two Halves of Program.cs</h4>\n<p>The file is always split into two distinct phases:</p>\n<ol>\n    <li><strong>The Builder Phase (Dependency Injection):</strong> Here, you use <code>builder.Services</code> to register all your application's dependencies (Database Contexts, Repositories, Third-Party APIs, Authentication mechanisms). You are essentially stocking the \"toolbox\" for your application.</li>\n    <li><strong>The App Phase (Middleware Pipeline):</strong> Once the builder creates the <code>app</code> object, you use <code>app.Use...</code> to define exactly how an incoming HTTP request should be processed. You define the order of operations: Should we log the request first? Should we check authentication next? Should we route to a controller?</li>\n</ol>",
    "code": "// PHASE 1: The Builder (Dependency Injection)\nvar builder = WebApplication.CreateBuilder(args);\n\n// Register framework services\nbuilder.Services.AddControllersWithViews(); // Adds MVC capabilities\nbuilder.Services.AddDbContext<AppDbContext>(); // Adds Entity Framework\n\n// Register your custom business logic\nbuilder.Services.AddScoped<IEmailService, MailchimpService>();\n\n// The Builder is finished. Create the application.\nvar app = builder.Build();\n\n// ==========================================\n\n// PHASE 2: The Pipeline (Middleware)\nif (!app.Environment.IsDevelopment())\n{\n    // In production, catch all errors cleanly\n    app.UseExceptionHandler(\"/Home/Error\");\n    app.UseHsts();\n}\n\napp.UseHttpsRedirection(); // Force HTTP to HTTPS\napp.UseStaticFiles();      // Enable wwwroot access\napp.UseRouting();          // Figure out which controller to use\napp.UseAuthorization();    // Ensure the user is allowed\n\napp.MapControllerRoute(\n    name: \"default\",\n    pattern: \"{controller=Home}/{action=Index}/{id?}\");\n\n// Block the main thread and start listening for web traffic!\napp.Run();",
    "summary": "Program.cs is the unified entry point divided into two sections: registering services into the Dependency Injection container, and configuring the HTTP Middleware Pipeline."
  },
  {
    "id": 7,
    "title": "7. .NET Core Command-Line Interface (CLI)",
    "category": "Phase 2: Project Structure & Configuration",
    "language": "bash",
    "explanation": "<h3>Mastering the Terminal</h3>\n<p>The <strong>.NET CLI (Command-Line Interface)</strong> is the engine that powers the entire .NET ecosystem. Even when you click \"Run\" inside a massive IDE like Visual Studio, it is actually just executing these CLI commands behind the scenes.</p>\n<p>Learning the CLI is essential because it allows you to build and deploy .NET applications on Linux servers, inside Docker containers, and via automated CI/CD pipelines (like GitHub Actions or Azure DevOps) where no visual IDE exists.</p>\n<h4>The Essential Commands Every Developer Must Know:</h4>\n<ul>\n    <li><code>dotnet new</code>: The scaffolding engine. Used to generate boilerplates. (e.g., <code>dotnet new webapi</code>, <code>dotnet new mvc</code>, <code>dotnet new console</code>, <code>dotnet new sln</code>).</li>\n    <li><code>dotnet build</code>: Compiles your C# code into a <code>.dll</code> assembly without running it. Great for checking for syntax errors.</li>\n    <li><code>dotnet run</code>: Compiles the code and immediately launches the Kestrel web server to test the app locally.</li>\n    <li><code>dotnet watch</code>: The holy grail for UI development. It starts the server, watches all your files for changes, and automatically restarts the app and refreshes your browser the moment you hit 'Save' (Hot Reload).</li>\n    <li><code>dotnet test</code>: Automatically finds and executes all unit tests in your solution.</li>\n    <li><code>dotnet publish</code>: The final step. It compiles your code, strips away developer tools, bundles all required NuGet packages, and prepares a highly optimized output folder ready to be copied to a production server.</li>\n</ul>",
    "code": "# Create a complete solution from scratch via CLI\ndotnet new sln -n MyEnterpriseApp\n\n# Create a Web API project\ndotnet new webapi -n MyEnterpriseApp.Api\n\n# Add the API project to the Solution\ndotnet sln add MyEnterpriseApp.Api\n\n# Add a third-party NuGet Package (Entity Framework)\ncd MyEnterpriseApp.Api\ndotnet add package Microsoft.EntityFrameworkCore.SqlServer\n\n# Run the application with Hot Reload\ndotnet watch run",
    "summary": "The .NET CLI is a powerful, cross-platform toolchain for scaffolding, compiling, testing, and deploying .NET applications without needing a visual IDE."
  },
  {
    "id": 8,
    "title": "8. Configure the Default File to be Served",
    "category": "Phase 2: Project Structure & Configuration",
    "language": "csharp",
    "explanation": "<h3>Landing Pages for Static Sites</h3>\n<p>Imagine you have a single-page application (like React or Angular) or a simple static website hosted inside your ASP.NET Core app. Your <code>wwwroot</code> folder contains an <code>index.html</code> file.</p>\n<p>When a user types <code>http://yourwebsite.com/</code> into their browser, the server receives a request for the root directory. By default, ASP.NET Core has <strong>no idea</strong> what file to serve when a directory is requested. It won't guess. It will simply return a 404 Not Found error.</p>\n<p>To fix this, you must explicitly enable <strong>Default Files Middleware</strong>.</p>\n<h4>How UseDefaultFiles Works</h4>\n<p>When you add <code>app.UseDefaultFiles()</code> to your pipeline, it intercepts requests for directories (like <code>/</code> or <code>/about/</code>). It then checks the directory inside <code>wwwroot</code> to see if any of these standard files exist:</p>\n<ul>\n    <li><code>default.htm</code></li>\n    <li><code>default.html</code></li>\n    <li><code>index.htm</code></li>\n    <li><code>index.html</code></li>\n</ul>\n<p>If it finds one (e.g., <code>index.html</code>), it seamlessly rewrites the incoming URL to <code>/index.html</code> and passes it to the next middleware.</p>",
    "code": "var builder = WebApplication.CreateBuilder(args);\nvar app = builder.Build();\n\n// CRITICAL ORDERING:\n// UseDefaultFiles must come BEFORE UseStaticFiles.\n// UseDefaultFiles rewrites the URL from \"/\" to \"/index.html\".\napp.UseDefaultFiles(); \n\n// UseStaticFiles then sees the request for \"/index.html\" \n// and physically serves the file from the wwwroot folder.\napp.UseStaticFiles();\n\napp.Run();",
    "mistake": "If you swap the order and put `UseStaticFiles()` first, it will look at the request for `/`, realize it's not a specific file, and ignore it. Then `UseDefaultFiles()` will rewrite the URL to `/index.html`, but the static files middleware has already passed, so the file will never be served!",
    "summary": "app.UseDefaultFiles() allows the server to automatically serve standard landing pages like index.html when a user requests a base directory URL."
  },
  {
    "id": 9,
    "title": "9. ASP.NET Core - Environment Variables",
    "category": "Phase 2: Project Structure & Configuration",
    "language": "csharp",
    "explanation": "<h3>Adapting to Surroundings</h3>\n<p>An enterprise application must behave differently depending on where it is currently running.</p>\n<ul>\n    <li>When running on your <strong>Local Laptop (Development)</strong>, it should connect to a local dummy database, show detailed error stack traces, and use mock APIs.</li>\n    <li>When running on a <strong>Live Server (Production)</strong>, it must connect to the highly secure cloud database, hide error details from users, and send emails via a real SMTP server.</li>\n</ul>\n<p>ASP.NET Core manages this via the <code>ASPNETCORE_ENVIRONMENT</code> variable. The framework looks at this variable the moment it starts up to determine its identity.</p>\n<h4>The Configuration Cascade</h4>\n<p>ASP.NET Core uses a brilliant overriding system for its JSON configuration files:</p>\n<ol>\n    <li>First, it loads the base <code>appsettings.json</code>. This contains global defaults.</li>\n    <li>Next, it checks the environment variable (e.g., \"Production\").</li>\n    <li>Finally, it looks for a file named <code>appsettings.Production.json</code>. Any settings in this file will completely overwrite the base settings!</li>\n</ol>",
    "code": "// Program.cs\nvar app = builder.Build();\n\n// The framework provides helper methods to check the environment\nif (app.Environment.IsDevelopment())\n{\n    // Dangerous: Shows full C# source code lines that caused errors\n    app.UseDeveloperExceptionPage();\n}\nelse if (app.Environment.IsStaging())\n{\n    // Custom logic for the staging server\n}\nelse \n{\n    // Production: Safe, generic error page for users\n    app.UseExceptionHandler(\"/Home/Error\");\n}",
    "tip": "You should almost never commit `appsettings.Production.json` containing real database passwords to GitHub. Instead, you inject the production passwords directly into the Azure App Service or Docker Container using actual OS-level environment variables, which ASP.NET Core reads automatically.",
    "summary": "Environment variables allow ASP.NET Core to dynamically adapt its configuration, connection strings, and error handling depending on whether it is running locally or in a live production environment."
  },
  {
    "id": 10,
    "title": "10. ASP.NET Core - Dependency Injection (DI)",
    "category": "Phase 3: Dependency Injection & Middleware",
    "language": "csharp",
    "explanation": "<h3>The Cure for Tightly Coupled Code</h3>\n<p>Before understanding <strong>Dependency Injection (DI)</strong>, you must understand the problem it solves: <em>Tight Coupling</em>.</p>\n<p>Imagine you have an <code>OrderController</code> that needs to save an order to the database. The amateur way to write this is to use the <code>new</code> keyword inside the controller: <code>var repo = new SqlOrderRepository();</code>.</p>\n<p>This is a disaster for three reasons:</p>\n<ol>\n    <li><strong>Testing is impossible:</strong> You cannot run a unit test on the controller without it literally connecting to the live SQL database.</li>\n    <li><strong>Rigidity:</strong> If you want to switch from SQL to MongoDB, you have to open the controller and rewrite the code.</li>\n    <li><strong>Dependency Chains:</strong> What if <code>SqlOrderRepository</code> requires an <code>ILogger</code> to be instantiated? Now your controller has to figure out how to create a logger just to create the repository!</li>\n</ol>\n<h4>The DI Solution</h4>\n<p>Instead of the controller fetching its tools, <strong>the tools are handed to it</strong>. You design the controller's constructor to accept an Interface (e.g., <code>IOrderRepository</code>). The controller says: <em>\"I don't know or care what database we are using. I just need something that fulfills this contract.\"</em></p>\n<p>In ASP.NET Core, DI is built into the foundation of the framework. You register the interface and the concrete implementation in <code>Program.cs</code>. When a web request hits the controller, the framework acts as a factory, automatically instantiating the <code>SqlOrderRepository</code> (and anything it needs) and injecting it into the controller's constructor.</p>",
    "diagram": "flowchart LR\n    Cont[\"OrderController\"] -.->|\"Depends Only On\"| Int((\"IOrderRepository\"))\n    IoC[\"Built-in DI Container\"] -->|\"1. Reads Interface\"| Int\n    IoC -->|\"2. Automatically Instantiates\"| SQL[\"SqlOrderRepository\"]\n    SQL -->|\"3. Injects Instance via Constructor\"| Cont\n    \n    style IoC fill:#f59e0b,color:#fff\n    style Cont fill:#3b82f6,color:#fff\n    style Int fill:#10b981,color:#fff",
    "code": "// 1. Register in Program.cs\nbuilder.Services.AddScoped<IOrderRepository, SqlOrderRepository>();\n\n// 2. Consume in the Controller\npublic class OrderController : ControllerBase\n{\n    private readonly IOrderRepository _repo;\n\n    // The framework AUTOMATICALLY provides this!\n    // No \"new\" keyword anywhere!\n    public OrderController(IOrderRepository repo)\n    {\n        _repo = repo;\n    }\n\n    [HttpPost]\n    public IActionResult Create(Order order)\n    {\n        _repo.Save(order); // Could be SQL, could be Mongo, could be a Mock!\n        return Ok();\n    }\n}",
    "summary": "Dependency Injection reverses the flow of control. Instead of classes creating their own dependencies, the framework injects them via constructors, resulting in highly testable, modular, and loosely-coupled code."
  },
  {
    "id": 11,
    "title": "11. Built-in IoC Container Service Lifetimes",
    "category": "Phase 3: Dependency Injection & Middleware",
    "language": "csharp",
    "explanation": "<h3>Controlling the Lifespan of Objects</h3>\n<p>When you register a service in the IoC (Inversion of Control) container, you must tell the framework exactly <em>how long</em> that object should live in memory before it is destroyed by the Garbage Collector. You do this by selecting a <strong>Service Lifetime</strong>.</p>\n<h4>The Three Lifetimes:</h4>\n<ol>\n    <li><strong>Transient (<code>AddTransient</code>)</strong>: The shortest lifespan. The container will create a brand new, unique instance of the object <em>every single time</em> it is requested. \n        <ul>\n            <li><strong>Use Case:</strong> Lightweight, stateless utility classes. (e.g., an HTML sanitizer or string formatter).</li>\n        </ul>\n    </li>\n    <li><strong>Scoped (<code>AddScoped</code>)</strong>: The \"Web\" lifespan. The container creates exactly one instance per HTTP Request. If three different classes ask for the service during the same web request, they all get the exact same instance. Once the request finishes, the object is destroyed.\n        <ul>\n            <li><strong>Use Case:</strong> Database Contexts (Entity Framework) and Repositories. You want all database operations in a single web request to share the same connection and transaction!</li>\n        </ul>\n    </li>\n    <li><strong>Singleton (<code>AddSingleton</code>)</strong>: The immortal lifespan. The container creates exactly one instance the very first time it is requested. That identical object remains in memory permanently and is shared by <em>every single user</em> and every single web request for the entire life of the application.\n        <ul>\n            <li><strong>Use Case:</strong> In-memory caches, configuration loaders, or connections to external message brokers (like RabbitMQ).</li>\n        </ul>\n    </li>\n</ol>",
    "code": "var builder = WebApplication.CreateBuilder(args);\n\n// Transient: 100 requests = 100 new objects created\nbuilder.Services.AddTransient<ITaxCalculator, TaxCalculator>();\n\n// Scoped: 1 request asking for repo 5 times = 1 object shared 5 times\nbuilder.Services.AddScoped<IOrderRepository, SqlOrderRepository>();\n\n// Singleton: 10,000 users over 5 days = 1 single object in memory\nbuilder.Services.AddSingleton<ICacheService, RedisCacheService>();",
    "mistake": "The 'Captive Dependency' anti-pattern: NEVER inject a Scoped service (like a database connection) into a Singleton service! The Singleton will lock the Scoped service in memory permanently, keeping the database connection open forever and causing the server to crash from connection exhaustion.",
    "summary": "The container manages memory automatically based on the lifetime you choose: Transient (always new), Scoped (shared per web request), or Singleton (shared globally forever)."
  },
  {
    "id": 12,
    "title": "12. ASP.NET Core - Middleware",
    "category": "Phase 3: Dependency Injection & Middleware",
    "language": "csharp",
    "explanation": "<h3>The HTTP Request Assembly Line</h3>\n<p>Imagine a car moving through a factory assembly line. Station 1 adds the engine, Station 2 adds the doors, and Station 3 paints it. If Station 1 realizes the chassis is broken, it stops the belt and rejects the car immediately.</p>\n<p>This is exactly how <strong>Middleware</strong> works in ASP.NET Core.</p>\n<p>When an HTTP Request hits your server, it doesn't just jump directly to your Controller. It flows through a strict pipeline of middleware components. Each piece of middleware gets to look at the request and decide what to do.</p>\n<h4>A Middleware Component can do three things:</h4>\n<ol>\n    <li><strong>Modify the Request/Response:</strong> It can attach a correlation ID, compress the output, or log the performance.</li>\n    <li><strong>Pass it to the next component:</strong> It calls <code>await next()</code> to hand the request to the next middleware in line.</li>\n    <li><strong>Short-Circuit the Pipeline:</strong> It can refuse to call <code>next()</code>. For example, the Authentication Middleware looks for a JWT token. If it doesn't find one, it immediately returns a 401 Unauthorized response, and the request NEVER reaches the Controller.</li>\n</ol>",
    "diagram": "flowchart LR\n    Req[\"Incoming HTTP Request\"] --> Exception[\"Exception Handling Middleware\"]\n    Exception --> Auth[\"Authentication Middleware\"]\n    Auth -.->|\"401 Unauthorized (Short-Circuit!)\"| Req\n    Auth --> MVC[\"Routing Middleware (Controllers)\"]\n    \n    MVC -.->|\"Generate JSON\"| Auth\n    Auth -.->|\"Pass Response Back\"| Exception\n    Exception -.->|\"HTTP Response\"| User((\"Client\"))\n    \n    style Auth fill:#ef4444,color:#fff\n    style MVC fill:#3b82f6,color:#fff",
    "tip": "The order in which you add middleware to `Program.cs` is critical! The pipeline executes sequentially top-to-bottom for requests, and bottom-to-top for responses. If you put `app.UseAuthorization()` before `app.UseAuthentication()`, the app will fail because you can't authorize a user before you authenticate who they are!",
    "summary": "Middleware forms an sequential pipeline that inspects, modifies, or short-circuits every incoming HTTP request and outgoing HTTP response."
  },
  {
    "id": 13,
    "title": "13. Add Custom Middleware in ASP.NET Core Application",
    "category": "Phase 3: Dependency Injection & Middleware",
    "language": "csharp",
    "explanation": "<h3>Writing Your Own Assembly Line Stations</h3>\n<p>While Microsoft provides dozens of built-in middleware (Static Files, Routing, CORS), you will often need to write custom middleware for enterprise requirements, such as injecting custom HTTP headers, global performance timing, or custom API key validation.</p>\n<h4>Two Ways to Build Middleware:</h4>\n<p><strong>1. Inline Middleware (Using <code>app.Use()</code>):</strong> This is written directly in <code>Program.cs</code>. It's fast and easy for simple tasks, taking an <code>HttpContext</code> and a <code>next</code> delegate.</p>\n<p><strong>2. Class-Based Middleware:</strong> For complex logic, you create a standalone C# class. This class must receive a <code>RequestDelegate</code> in its constructor, and contain a method named <code>Invoke</code> or <code>InvokeAsync</code>.</p>",
    "code": "// Method 1: Inline Custom Middleware in Program.cs\napp.Use(async (context, next) =>\n{\n    // 1. INBOUND: Do work BEFORE the next middleware\n    var watch = System.Diagnostics.Stopwatch.StartNew();\n    context.Response.Headers.Add(\"X-Developed-By\", \"VinnerTECH\");\n    \n    // 2. Pass control down the pipeline\n    await next.Invoke();\n    \n    // 3. OUTBOUND: Do work AFTER the next middleware has finished generating the response\n    watch.Stop();\n    Console.WriteLine($\"Request took {watch.ElapsedMilliseconds} ms\");\n});",
    "mistake": "Never write to the response body or modify headers AFTER calling `await next.Invoke()`. Once `next()` finishes, the response has already started sending to the client, and attempting to modify headers will throw a severe runtime exception.",
    "summary": "Custom middleware allows you to wrap every single HTTP request with your own logic, giving you absolute control over the inbound request and outbound response."
  },
  {
    "id": 14,
    "title": "14. ASP.NET Core - Run vs Use vs Map",
    "category": "Phase 3: Dependency Injection & Middleware",
    "language": "csharp",
    "explanation": "<h3>Controlling the Flow of the Pipeline</h3>\n<p>When you configure the middleware pipeline in <code>Program.cs</code>, you have three primary extension methods at your disposal to control exactly how the request flows.</p>\n<h4>1. <code>app.Use()</code> - The Chainer</h4>\n<p><code>Use</code> adds a piece of middleware that has the ability to call the <em>next</em> piece of middleware in the sequence. Most of your pipeline is built with <code>Use</code>.</p>\n<h4>2. <code>app.Run()</code> - The Terminator</h4>\n<p><code>Run</code> adds a <strong>Terminal Middleware</strong>. It does not accept a <code>next</code> delegate. It handles the request completely and immediately reverses the pipeline. If you put <code>app.Run()</code> in the middle of your pipeline, no middleware registered below it will ever execute!</p>\n<h4>3. <code>app.Map()</code> - The Brancher</h4>\n<p><code>Map</code> is used to create a completely separate, isolated branch of the pipeline based on the URL path. If the URL matches, the request is routed down this new branch and will NEVER return to the main pipeline.</p>",
    "code": "var app = builder.Build();\n\n// 1. MAP: If URL starts with \"/api\", go down this branch\napp.Map(\"/api\", apiApp => {\n    // This is a separate mini-pipeline!\n    apiApp.Use(async (context, next) => { /* API Logic */ });\n    apiApp.Run(async context => await context.Response.WriteAsync(\"API End\"));\n});\n\n// 2. USE: Standard middleware in the main pipeline\napp.Use(async (context, next) => {\n    Console.WriteLine(\"Passing through...\");\n    await next(); \n});\n\n// 3. RUN: The absolute end of the main pipeline\napp.Run(async context => {\n    await context.Response.WriteAsync(\"Hello from the Terminal Run!\");\n});",
    "summary": "Use() chains middleware together, Run() terminates the pipeline immediately, and Map() splits the pipeline into isolated branches based on the URL."
  },
  {
    "id": 15,
    "title": "15. ASP.NET Core - Exception Handling Middleware",
    "category": "Phase 3: Dependency Injection & Middleware",
    "language": "csharp",
    "explanation": "<h3>Catching Every Crash Globally</h3>\n<p>In legacy applications, developers used to scatter <code>try-catch</code> blocks across hundreds of controller methods to ensure the app wouldn't crash. In ASP.NET Core, this is considered a massive anti-pattern.</p>\n<p>Because the middleware pipeline is like an onion (the request travels through the layers in, and the response travels through the layers out), you can put an Exception Handling Middleware at the very outermost layer. If a controller deep in the center throws an unhandled exception, it will bubble all the way out until the Exception Middleware catches it.</p>\n<h4>Why is this critical?</h4>\n<p>If an exception leaks to the user's browser, they will see a Yellow Screen of Death (YSOD) containing raw C# code, database connection strings, and server file paths. This is a severe security vulnerability.</p>",
    "code": "var app = builder.Build();\n\n// MUST BE FIRST IN THE PIPELINE!\nif (app.Environment.IsDevelopment())\n{\n    // Developer gets to see the raw stack trace for debugging\n    app.UseDeveloperExceptionPage(); \n}\nelse\n{\n    // 1. Catch the crash\n    // 2. Log it silently\n    // 3. Redirect the user to a clean, generic HTML error page\n    app.UseExceptionHandler(\"/Home/Error\");\n    \n    // Enforce strict HTTPS security headers in production\n    app.UseHsts(); \n}\n\n// ... other middleware ...",
    "tip": "Always place `app.UseExceptionHandler()` as the very first middleware registered. It can only catch exceptions thrown by middleware registered *after* it. If it is registered last, it will catch nothing.",
    "summary": "Global exception middleware eliminates the need for scattered try-catch blocks. It catches all unhandled errors, preventing sensitive stack traces from leaking to end users in production."
  },
  {
    "id": 16,
    "title": "16. ASP.NET Core - Serving Static Files",
    "category": "Phase 4: Static Files & Logging",
    "language": "csharp",
    "explanation": "<h3>Opening the Security Gate</h3>\n<p>As discussed earlier, the <code>wwwroot</code> folder is the designated safe zone for static files (CSS, JS, Images). However, ASP.NET Core is \"secure by default.\" Even if you put a file in <code>wwwroot</code>, the web server will absolutely refuse to serve it to the public.</p>\n<p>To open the gate and allow the browser to download these files, you must add the <strong>Static Files Middleware</strong> to your pipeline.</p>\n<h4>How it works under the hood:</h4>\n<p>When an HTTP request comes in (e.g., <code>GET /css/style.css</code>), the pipeline passes it down until it hits <code>app.UseStaticFiles()</code>.</p>\n<ul>\n    <li>This middleware looks at the URL path (<code>/css/style.css</code>).</li>\n    <li>It checks the physical <code>wwwroot</code> folder on the hard drive.</li>\n    <li>If it finds the file, it reads it, streams it back to the user as an HTTP Response, and <strong>short-circuits</strong> the pipeline (no further middleware or controllers are executed).</li>\n    <li>If it does NOT find the file, it quietly calls <code>next()</code>, allowing the request to continue down the pipeline to see if a Controller can handle it.</li>\n</ul>",
    "code": "var app = builder.Build();\n\n// Enable serving files from the wwwroot folder\napp.UseStaticFiles();\n\napp.MapControllers();\napp.Run();",
    "summary": "You must explicitly call app.UseStaticFiles() to authorize the Kestrel web server to transmit static files from the wwwroot directory to the user's browser."
  },
  {
    "id": 17,
    "title": "17. Serving Static Files Outside wwwroot",
    "category": "Phase 4: Static Files & Logging",
    "language": "csharp",
    "explanation": "<h3>Exposing Custom Hard Drive Folders</h3>\n<p>While <code>wwwroot</code> is the standard, enterprise applications often need to serve static files from entirely different locations on the server. For example, a shared corporate <code>Z:\\Images</code> drive containing millions of product photos, or a Node.js <code>node_modules</code> folder during development.</p>\n<p>You can configure the Static Files middleware to map a specific physical folder path to a specific URL alias on the internet.</p>\n<h4>The Configuration Object</h4>\n<p>You achieve this by passing a <code>StaticFileOptions</code> object into the middleware. You must define two things:</p>\n<ol>\n    <li><strong>FileProvider:</strong> The actual physical path on the server's hard drive.</li>\n    <li><strong>RequestPath:</strong> The fake URL alias the browser will use to request the files.</li>\n</ol>",
    "code": "using Microsoft.Extensions.FileProviders;\n\nvar app = builder.Build();\n\n// 1. Serve default wwwroot files (Standard)\napp.UseStaticFiles(); \n\n// 2. Serve files from a custom physical folder\napp.UseStaticFiles(new StaticFileOptions\n{\n    // The physical location on the hard drive\n    FileProvider = new PhysicalFileProvider(@\"C:CorporateSharesMyCustomFiles\"),\n    \n    // The URL path the browser uses\n    RequestPath = \"/CorporateAssets\"\n});\n\napp.Run();",
    "example": "With this configuration, if you have a file physically located at `C:\\CorporateShares\\MyCustomFiles\\banner.jpg`, the browser can download it by navigating to `http://yourwebsite.com/CorporateAssets/banner.jpg`.",
    "summary": "By configuring StaticFileOptions with a PhysicalFileProvider, you can safely expose any hard drive directory to the web under a custom URL alias."
  },
  {
    "id": 18,
    "title": "18. Fundamentals of Logging in .NET Core",
    "category": "Phase 4: Static Files & Logging",
    "language": "json",
    "explanation": "<h3>The Black Box Recorder</h3>\n<p>When your application is running in production and a user clicks a button and nothing happens, how do you know what went wrong? You can't attach a debugger to a live production server. You rely entirely on <strong>Logs</strong>.</p>\n<p>Logging is built natively into the absolute core of .NET. The framework provides an <code>ILogger&lt;T&gt;</code> interface that you can inject anywhere.</p>\n<h4>The 6 Log Levels (Severity)</h4>\n<ol>\n    <li><strong>Trace (0):</strong> The most microscopic details. Often contains sensitive passwords or full HTTP bodies. Never enabled in production.</li>\n    <li><strong>Debug (1):</strong> Helpful info during development only.</li>\n    <li><strong>Information (2):</strong> Tracking the normal flow of the app. (e.g., \"User 45 logged in\", \"Payment processed\").</li>\n    <li><strong>Warning (3):</strong> Abnormal but recoverable events. (e.g., \"Database query took 4 seconds\", \"File not found, using default\").</li>\n    <li><strong>Error (4):</strong> A failure in the current execution flow. The user's action failed. (e.g., \"Database timeout\", \"Null reference exception\").</li>\n    <li><strong>Critical (5):</strong> The entire system is crashing or completely unusable. (e.g., \"Out of memory\", \"Disk full\").</li>\n</ol>",
    "code": "// appsettings.json\n{\n  \"Logging\": {\n    \"LogLevel\": {\n      // Show Information and above for my own code\n      \"Default\": \"Information\",\n      // But only show Warnings and above from the framework \n      // (Hides the massive amount of routing/SQL logs generated by Microsoft)\n      \"Microsoft\": \"Warning\",\n      \"Microsoft.Hosting.Lifetime\": \"Information\"\n    }\n  }\n}",
    "tip": "In a production `appsettings.Production.json`, you should set the Default LogLevel to `Warning`. If you leave it at `Information`, a high-traffic app will generate gigabytes of text logs per day, rapidly filling up your server's hard drive and crashing the machine.",
    "summary": "The ILogger interface provides native, leveled logging. By adjusting the LogLevel in appsettings.json, you control exactly how much diagnostic data is written to the disk."
  },
  {
    "id": 19,
    "title": "19. Implementing Structured Logging",
    "category": "Phase 4: Static Files & Logging",
    "language": "csharp",
    "explanation": "<h3>Writing Logs the Enterprise Way</h3>\n<p>To write logs in ASP.NET Core, you simply inject <code>ILogger&lt;YourClassName&gt;</code> into the constructor. The generic type <code>&lt;YourClassName&gt;</code> automatically attaches the name of the class to the log output, so you know exactly which file generated the log.</p>\n<h4>The Golden Rule: Structured Logging</h4>\n<p>When you write a log message, you must NOT use standard C# string concatenation (<code>+</code>) or string interpolation (<code>$\"{...}\"</code>). You must use <strong>Message Templates</strong>.</p>\n<p>Why? In production, you don't read logs in Notepad. You send them to intelligent cloud aggregators (like Datadog, Splunk, or Application Insights). If you use string interpolation, the cloud aggregator just sees a massive blob of text. If you use a Message Template, the aggregator extracts the variables into searchable database columns!</p>",
    "code": "public class PaymentController : ControllerBase\n{\n    private readonly ILogger<PaymentController> _logger;\n\n    public PaymentController(ILogger<PaymentController> logger)\n    {\n        _logger = logger;\n    }\n\n    [HttpPost]\n    public IActionResult Process(int amount, string user)\n    {\n        // ❌ THE WRONG WAY (String Interpolation)\n        // Aggregator sees text: \"User Bob paid 500\" - Unsearchable!\n        _logger.LogInformation($\"User {user} paid {amount}\");\n        \n        // ✅ THE RIGHT WAY (Message Template / Structured Logging)\n        // Aggregator sees text: \"User Bob paid 500\"\n        // Aggregator extracts variables: { \"User\": \"Bob\", \"Amount\": 500 } - SEARCHABLE!\n        _logger.LogInformation(\"User {User} paid {Amount}\", user, amount);\n        \n        try\n        {\n            // Logic...\n        }\n        catch (Exception ex)\n        {\n            // Always pass the Exception object as the FIRST parameter\n            _logger.LogError(ex, \"Failed to process payment for {User}\", user);\n            return StatusCode(500);\n        }\n        \n        return Ok();\n    }\n}",
    "summary": "Inject ILogger<T> into your classes to write logs. Always use Message Templates instead of string interpolation so cloud monitoring tools can index your log variables for advanced searching."
  },
  {
    "id": 20,
    "title": "20. .NET Application Types",
    "category": "Phase 5: Frameworks & Code Sharing",
    "language": "csharp",
    "explanation": "<h3>The Right Tool for the Job</h3>\n<p>When you open Visual Studio or use the .NET CLI, you are presented with dozens of \"Project Templates\". .NET is a universal platform, meaning you can build almost any type of software with C#.</p>\n<h4>Web Development Models:</h4>\n<ol>\n    <li><strong>ASP.NET Core Web API:</strong> The backbone of modern architecture. It has no UI. It simply accepts HTTP requests and returns raw JSON data. It is designed to be the \"Backend\" that serves a React, Angular, iOS, or Android frontend.</li>\n    <li><strong>ASP.NET Core MVC (Model-View-Controller):</strong> The traditional approach. The server handles the logic and uses Razor syntax to generate and return a full HTML web page.</li>\n    <li><strong>ASP.NET Core Razor Pages:</strong> A more modern, streamlined alternative to MVC for page-focused apps. Instead of complex routing between Controllers and Views, each web page has its own dedicated <code>.cshtml</code> (UI) and <code>.cshtml.cs</code> (Logic) file.</li>\n    <li><strong>Blazor WebAssembly:</strong> A revolutionary framework that compiles C# directly into WebAssembly. This allows you to write interactive, client-side UI code in C# instead of JavaScript, running natively inside the user's browser.</li>\n</ol>\n<h4>Other Common Models:</h4>\n<ul>\n    <li><strong>Console Application:</strong> Command-line tools or background worker services.</li>\n    <li><strong>MAUI (Multi-platform App UI):</strong> Builds native desktop and mobile apps (Windows, Mac, iOS, Android) from a single C# codebase.</li>\n</ul>",
    "summary": ".NET provides specialized templates for every scenario, from headless JSON APIs to full-stack WebAssembly applications and native mobile apps."
  },
  {
    "id": 21,
    "title": "21. Code Sharing: .NET Standard vs .NET",
    "category": "Phase 5: Frameworks & Code Sharing",
    "language": "csharp",
    "explanation": "<h3>Don't Repeat Yourself (DRY)</h3>\n<p>In enterprise software, you rarely build just one project. You might have an ASP.NET Web API (for the web), a MAUI App (for iOS), and a Worker Service (for background processing).</p>\n<p>If all three of these applications need to interact with a <code>User</code> model or calculate taxes, you do NOT want to copy and paste that C# code into three different projects. Instead, you create a <strong>Class Library</strong>.</p>\n<h4>The Evolution of Class Libraries</h4>\n<p>Historically, Microsoft had completely different runtimes: the legacy .NET Framework (Windows only), .NET Core (Cross-platform), and Xamarin (Mobile). A library compiled for Windows wouldn't run on Mobile.</p>\n<p>To solve this, Microsoft created <strong>.NET Standard</strong>. If you built a \".NET Standard Class Library\", you were restricted to a common subset of APIs, but it was guaranteed to work across all runtimes.</p>\n<p><strong>Today, things are much simpler.</strong> With the release of .NET 5 (and now .NET 8+), Microsoft successfully merged all runtimes into one unified platform. You no longer need to worry about .NET Standard. You simply create a standard <strong>.NET Class Library</strong>, and it can be instantly referenced and shared by any modern .NET application, anywhere.</p>",
    "diagram": "flowchart TD\n    Lib[\"SharedBusinessLogic.dll (Class Library)\"]\n    API[\"ASP.NET Web API\"] -->|\"References\"| Lib\n    Mobile[\"MAUI iOS App\"] -->|\"References\"| Lib\n    Worker[\"Background Windows Service\"] -->|\"References\"| Lib\n    \n    style Lib fill:#10b981,color:#fff\n    style API fill:#3b82f6,color:#fff\n    style Mobile fill:#3b82f6,color:#fff\n    style Worker fill:#3b82f6,color:#fff",
    "summary": "Class Libraries allow you to extract common models and business logic into a separate DLL, which can then be shared and consumed by multiple different applications."
  },
  {
    "id": 22,
    "title": "22. Target Multiple Frameworks (Multi-targeting)",
    "category": "Phase 5: Frameworks & Code Sharing",
    "language": "xml",
    "explanation": "<h3>One Project, Multiple Outputs</h3>\n<p>If you are a normal developer building an internal company app, you will never need this. But if you are building an open-source library (like a NuGet package) intended for thousands of developers, you face a problem:</p>\n<p>Some developers are stuck on the ancient legacy .NET Framework 4.8. Others are using the cutting-edge .NET 8. How do you write a library that works for both without maintaining two completely separate codebases?</p>\n<p>The solution is <strong>Multi-targeting</strong>. By editing your project's <code>.csproj</code> file, you can instruct the compiler to build your code multiple times, producing different DLLs for different frameworks.</p>\n<h4>How it works:</h4>\n<p>You change the singular <code>&lt;TargetFramework&gt;</code> XML node to the plural <code>&lt;TargetFrameworks&gt;</code> and list the monikers separated by semicolons.</p>",
    "code": "<!-- MyOpenSourceLibrary.csproj -->\n<Project Sdk=\"Microsoft.NET.Sdk\">\n\n  <PropertyGroup>\n    <!-- We are instructing the compiler to build this twice:\n         Once for legacy Framework 4.8 (net48)\n         Once for modern .NET 8 (net8.0) -->\n    <TargetFrameworks>net48;net8.0</TargetFrameworks>\n  </PropertyGroup>\n\n</Project>",
    "tip": "When multi-targeting, you can use Preprocessor Directives (`#if NET8_0`) directly inside your C# code. This allows you to use shiny new C# features when compiling for .NET 8, but fallback to older, compatible code when the compiler is generating the .NET 4.8 version.",
    "summary": "Multi-targeting allows package creators to build a single codebase into multiple compatible DLLs, supporting both legacy and modern .NET ecosystems simultaneously."
  },
  {
    "id": 23,
    "title": "23. CRUD with ASP.NET Core, SQL, and ADO.NET",
    "category": "Phase 6: Data Access & CRUD Applications",
    "language": "csharp",
    "explanation": "<h3>The Bare Metal Approach</h3>\n<p><strong>CRUD</strong> stands for Create, Read, Update, and Delete. These are the four fundamental operations of any persistent storage application.</p>\n<p>Before modern Object-Relational Mappers (ORMs) existed, developers interacted with SQL Server using <strong>ADO.NET</strong>. This is the lowest-level database technology in .NET. It forces you to write raw SQL strings, manually open network connections, and read data byte-by-byte from a data stream.</p>\n<h4>Why use ADO.NET today?</h4>\n<p>While extremely verbose and tedious to write, ADO.NET is incredibly fast because there are no abstraction layers weighing it down. It is often used in high-frequency trading apps or systems requiring absolute maximum performance.</p>\n<h4>The Core ADO.NET Objects:</h4>\n<ul>\n    <li><strong><code>SqlConnection</code></strong>: Establishes the physical network tunnel to the database using a Connection String.</li>\n    <li><strong><code>SqlCommand</code></strong>: The vehicle that carries your SQL string over the connection.</li>\n    <li><strong><code>SqlDataReader</code></strong>: A forward-only stream that reads the results coming back from a SELECT query one row at a time.</li>\n</ul>",
    "code": "public List<User> GetUsers()\n{\n    var users = new List<User>();\n    string connStr = _configuration.GetConnectionString(\"Default\");\n    \n    // 1. Establish the connection (Using block ensures it closes automatically)\n    using (var conn = new SqlConnection(connStr))\n    {\n        // 2. Prepare the SQL command\n        string sql = \"SELECT Id, Name FROM Users WHERE IsActive = 1\";\n        using (var cmd = new SqlCommand(sql, conn))\n        {\n            // 3. Open the network tunnel\n            conn.Open();\n            \n            // 4. Execute the command and read the stream\n            using (SqlDataReader reader = cmd.ExecuteReader())\n            {\n                // reader.Read() moves to the next row, returns false when done\n                while (reader.Read())\n                {\n                    // 5. Manually map the database columns to the C# object\n                    users.Add(new User {\n                        Id = Convert.ToInt32(reader[\"Id\"]),\n                        Name = reader[\"Name\"].ToString()\n                    });\n                }\n            }\n        }\n    }\n    return users;\n}",
    "mistake": "NEVER build your SQL strings by concatenating user input (e.g., `\"SELECT * FROM Users WHERE Name = '\" + userInput + \"'\"`). This is the number one cause of SQL Injection attacks, which will result in your entire database being deleted or stolen. ALWAYS use `SqlParameter` objects.",
    "summary": "ADO.NET requires manual SQL strings and mapping. It is verbose and error-prone, but offers the absolute highest performance for database operations."
  },
  {
    "id": 24,
    "title": "24. Entity Framework Core: DB-First Approach",
    "category": "Phase 6: Data Access & CRUD Applications",
    "language": "bash",
    "explanation": "<h3>Letting the Database Drive the Code</h3>\n<p>Because ADO.NET is so tedious, developers use <strong>Object-Relational Mappers (ORMs)</strong> like Entity Framework (EF) Core. An ORM bridges the gap between the Relational world (SQL tables) and the Object-Oriented world (C# Classes), allowing you to interact with the database entirely through C# and LINQ, without writing a single line of SQL.</p>\n<h4>The Database-First Workflow</h4>\n<p>In many enterprise scenarios, the database already exists. It was built by a DBA (Database Administrator) five years ago, and now you are tasked with building a modern ASP.NET Core API on top of it.</p>\n<p>Instead of manually typing out hundreds of C# classes to match the existing SQL tables, you use the <strong>Scaffold-DbContext</strong> command. This tool connects to your live database, analyzes the schema, and automatically reverse-engineers it into C# Model classes and a <code>DbContext</code> configuration file.</p>",
    "diagram": "flowchart LR\n    SQL[(Existing SQL Server Database)] -->|\"dotnet ef dbcontext scaffold\"| CLI[\"EF Core CLI Tools\"]\n    CLI -->|\"Generates\"| Models[\"C# Models (e.g., User.cs, Order.cs)\"]\n    CLI -->|\"Generates\"| Context[\"AppDbContext.cs (Configuration)\"]\n    \n    style SQL fill:#10b981,color:#fff\n    style CLI fill:#f59e0b,color:#fff\n    style Models fill:#3b82f6,color:#fff",
    "code": "# Run this in the .NET CLI at the root of your project\n# It connects to the database, reads it, and outputs the C# files into the \"Models\" folder\ndotnet ef dbcontext scaffold \"Server=myserver;Database=MyDb;Trusted_Connection=True;\" Microsoft.EntityFrameworkCore.SqlServer -o Models",
    "tip": "If the DBA alters the database later (e.g., adds a new column to the Users table), you simply re-run the Scaffold command with the `--force` flag to overwrite your C# models with the latest schema.",
    "summary": "Database-First utilizes the Scaffold command to connect to a pre-existing SQL database and automatically generate the corresponding C# Models."
  },
  {
    "id": 25,
    "title": "25. Entity Framework Core: Code-First Approach",
    "category": "Phase 6: Data Access & CRUD Applications",
    "language": "csharp",
    "explanation": "<h3>Letting the Code Drive the Database</h3>\n<p>The <strong>Code-First Approach</strong> is the modern industry standard for brand-new ASP.NET Core applications. It completely flips the paradigm: you never open SQL Server Management Studio to create tables manually.</p>\n<h4>The Workflow:</h4>\n<ol>\n    <li><strong>Write C#:</strong> You design your domain by writing pure C# classes (e.g., an <code>Article</code> class with an <code>Id</code> and <code>Title</code>).</li>\n    <li><strong>Create Migrations:</strong> You run a CLI command. EF Core looks at your C# classes, realizes the database doesn't have a table for them, and generates a \"Migration\" file. A Migration is simply a C# file containing the instructions on how to build the SQL table.</li>\n    <li><strong>Update Database:</strong> You run a second CLI command. EF Core reads the Migration file, translates it into raw SQL (e.g., <code>CREATE TABLE Articles...</code>), and executes it against the database.</li>\n</ol>\n<h4>Why is this superior?</h4>\n<p>Because your entire database schema is now represented as C# Migration files, your database structure is tracked in Git! If a new developer joins your team, they clone the repo, run one command, and EF Core builds the entire database on their local machine instantly.</p>",
    "code": "// 1. You write this simple C# Class\npublic class Article\n{\n    public int Id { get; set; } // EF automatically makes this the Primary Key\n    public string Title { get; set; }\n}\n\n// 2. You register it in the DbContext\npublic class AppDbContext : DbContext\n{\n    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options) { }\n    \n    // This tells EF Core to create a SQL table named \"Articles\"\n    public DbSet<Article> Articles { get; set; }\n}",
    "example": "To apply this, you run `dotnet ef migrations add InitialCreate` followed by `dotnet ef database update`. EF Core will connect to SQL Server and automatically create the 'Articles' table with an 'Id' INT column and a 'Title' NVARCHAR column.",
    "summary": "Code-First allows you to design your application entirely in C#. EF Core's Migration engine automatically generates and executes the SQL required to keep the database perfectly synced with your code."
  },
  {
    "id": 26,
    "title": "26. Keyed Services in Dependency Injection (.NET 8+)",
    "category": "Phase 3: Dependency Injection Mastery",
    "language": "csharp",
    "explanation": "<h3>Resolving Multiple Implementations of the Same Interface</h3>\n<p>Prior to .NET 8, if you registered multiple implementations of the same interface (e.g., <code>IStorageService</code> implemented by both <code>AzureBlobStorage</code> and <code>LocalStorage</code>), injecting <code>IStorageService</code> would only resolve the *last* registered service.</p>\n<p>.NET 8 introduced <strong>Keyed Services</strong> natively into the DI container. You can now register and resolve services using a unique key (string, enum, or object).</p>\n<h4>When to use Keyed Services?</h4>\n<ul>\n    <li>Multi-tenant applications requiring different implementations per tenant.</li>\n    <li>Payment gateways (e.g., resolving <code>IPaymentGateway</code> with key <code>\"Stripe\"</code> vs <code>\"PayPal\"</code>).</li>\n    <li>Caching providers (resolving <code>ICacheService</code> with key <code>\"Redis\"</code> vs <code>\"InMemory\"</code>).</li>\n</ul>",
    "diagram": "flowchart TD\n    Client[\"OrderController\"] -->|\"Injects [FromKeyedServices('Stripe')]\"| DI[\"DI Container\"]\n    DI --> S[\"StripePaymentGateway\"]\n    Client2[\"CheckoutController\"] -->|\"Injects [FromKeyedServices('PayPal')]\"| DI\n    DI --> P[\"PayPalPaymentGateway\"]\n    \n    style Client fill:#3b82f6,color:#fff\n    style DI fill:#8b5cf6,color:#fff\n    style S fill:#10b981,color:#fff\n    style P fill:#f59e0b,color:#fff",
    "code": "// 1. Register Keyed Services in Program.cs\nvar builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddKeyedScoped<IPaymentGateway, StripePaymentGateway>(\"stripe\");\nbuilder.Services.AddKeyedScoped<IPaymentGateway, PayPalPaymentGateway>(\"paypal\");\n\nvar app = builder.Build();\n\n// 2. Resolve via Controller Constructor or Action Parameter\npublic class CheckoutController : ControllerBase\n{\n    private readonly IPaymentGateway _stripeGateway;\n    private readonly IPaymentGateway _paypalGateway;\n\n    public CheckoutController(\n        [FromKeyedServices(\"stripe\")] IPaymentGateway stripeGateway,\n        [FromKeyedServices(\"paypal\")] IPaymentGateway paypalGateway)\n    {\n        _stripeGateway = stripeGateway;\n        _paypalGateway = paypalGateway;\n    }\n\n    [HttpPost(\"process-stripe\")]\n    public async Task<IActionResult> PayWithStripe([FromBody] PaymentRequest request)\n    {\n        var result = await _stripeGateway.ProcessPaymentAsync(request.Amount);\n        return Ok(result);\n    }\n}",
    "tip": "Keyed services eliminate the need for complex custom factory classes or third-party containers like Autofac when you need multiple implementations of an interface.",
    "summary": ".NET 8 Keyed Services allow registering and resolving distinct service implementations of the same interface using named keys."
  },
  {
    "id": 27,
    "title": "27. Minimal APIs Architecture vs Controllers",
    "category": "Phase 4: Web API & Routing",
    "language": "csharp",
    "explanation": "<h3>Lightweight, High-Throughput HTTP Endpoints</h3>\n<p><strong>Minimal APIs</strong> were introduced in .NET 6 to architect HTTP APIs with minimal boilerplate, ceremony, and overhead. Instead of creating Controller classes, Action methods, and attributes, you map endpoints directly in <code>Program.cs</code> or dedicated extension classes.</p>\n<h4>Key Architectural Differences:</h4>\n<table class=\"table table-bordered\">\n  <thead><tr><th>Feature</th><th>Minimal APIs</th><th>Controller-Based APIs</th></tr></thead>\n  <tbody>\n    <tr><td><strong>Startup Time & Memory</strong></td><td>Faster cold start, lower memory (skips MVC model-binding reflection)</td><td>Slightly heavier due to controller discovery</td></tr>\n    <tr><td><strong>Code Structure</strong></td><td>Functional endpoint mappings (<code>app.MapGet</code>)</td><td>Object-oriented class-based organization</td></tr>\n    <tr><td><strong>Filters</strong></td><td>Endpoint Filters (<code>AddEndpointFilter</code>)</td><td>Action, Resource, and Result Filters</td></tr>\n    <tr><td><strong>Best Use Case</strong></td><td>Microservices, High-throughput APIs, Serverless (Azure Functions)</td><td>Large enterprise monoliths with complex validation & UI</td></tr>\n  </tbody>\n</table>",
    "diagram": "flowchart LR\n    Req[\"Incoming HTTP Request\"] --> Router[\"ASP.NET Core Endpoint Router\"]\n    Router -->|\"Minimal API\"| Handler[\"Direct Lambda / Method Handler (Zero Reflection)\"]\n    Router -->|\"Controller API\"| Controller[\"Controller Factory -> Action Invoker -> Controller Instance\"]\n    \n    style Req fill:#3b82f6,color:#fff\n    style Handler fill:#10b981,color:#fff\n    style Controller fill:#f59e0b,color:#fff",
    "code": "// Program.cs - Minimal API Architecture with Route Groups & TypedResults\nvar builder = WebApplication.CreateBuilder(args);\nbuilder.Services.AddScoped<IProductService, ProductService>();\n\nvar app = builder.Build();\n\n// Create Route Group with common prefix and authorization\nvar productsGroup = app.MapGroup(\"/api/products\")\n                       .WithTags(\"Products\")\n                       .RequireAuthorization();\n\n// GET /api/products\nproductsGroup.MapGet(\"/\", async (IProductService svc, CancellationToken ct) =>\n{\n    var products = await svc.GetAllAsync(ct);\n    return TypedResults.Ok(products);\n});\n\n// GET /api/products/{id:int}\nproductsGroup.MapGet(\"/{id:int}\", async (int id, IProductService svc, CancellationToken ct) =>\n{\n    var product = await svc.GetByIdAsync(id, ct);\n    return product is not null \n        ? TypedResults.Ok(product) \n        : (IResult)TypedResults.NotFound(new { Message = $\"Product {id} not found\" });\n});\n\n// POST /api/products with Endpoint Filter validation\nproductsGroup.MapPost(\"/\", async (CreateProductDto dto, IProductService svc) =>\n{\n    var created = await svc.CreateAsync(dto);\n    return TypedResults.Created($\"/api/products/{created.Id}\", created);\n}).AddEndpointFilter(async (context, next) =>\n{\n    var dto = context.GetArgument<CreateProductDto>(0);\n    if (string.IsNullOrWhiteSpace(dto.Name) || dto.Price <= 0)\n    {\n        return TypedResults.BadRequest(\"Invalid product name or price.\");\n    }\n    return await next(context);\n});\n\napp.Run();",
    "summary": "Minimal APIs provide a high-performance, lightweight approach to building REST endpoints with route grouping, TypedResults, and endpoint filters."
  },
  {
    "id": 28,
    "title": "28. Global Exception Handling with IExceptionHandler (.NET 8+)",
    "category": "Phase 5: Error Handling & Resilience",
    "language": "csharp",
    "explanation": "<h3>The Modern Standard for Centralized Error Handling</h3>\n<p>In production applications, unhandled exceptions must never expose raw stack traces or database schema details to the client. In .NET 8+, Microsoft introduced the <strong><code>IExceptionHandler</code></strong> interface, replacing custom exception middleware with a standardized, performant pattern.</p>\n<p>It automatically formats error responses according to the <strong>RFC 7807 Problem Details specification</strong>, providing standard HTTP error structures (<code>title</code>, <code>status</code>, <code>detail</code>, <code>instance</code>).</p>",
    "diagram": "flowchart TD\n    Req[\"Request\"] --> Endpoint[\"Action / Minimal API\"]\n    Endpoint -->|\"Throws Unhandled Exception\"| Handler[\"GlobalExceptionHandler (IExceptionHandler)\"]\n    Handler --> Log[\"Logs Error with Serilog / Seq\"]\n    Handler --> Problem[\"Formats RFC 7807 ProblemDetails JSON\"]\n    Problem --> Client[\"Returns Clean 500 / 404 Response to Client\"]\n    \n    style Req fill:#3b82f6,color:#fff\n    style Endpoint fill:#ef4444,color:#fff\n    style Handler fill:#8b5cf6,color:#fff\n    style Client fill:#10b981,color:#fff",
    "code": "// 1. Create the Custom Exception Handler\nusing Microsoft.AspNetCore.Diagnostics;\nusing Microsoft.AspNetCore.Mvc;\n\npublic sealed class GlobalExceptionHandler : IExceptionHandler\n{\n    private readonly ILogger<GlobalExceptionHandler> _logger;\n\n    public GlobalExceptionHandler(ILogger<GlobalExceptionHandler> logger)\n    {\n        _logger = logger;\n    }\n\n    public async ValueTask<bool> TryHandleAsync(\n        HttpContext httpContext,\n        Exception exception,\n        CancellationToken cancellationToken)\n    {\n        _logger.LogError(exception, \"Unhandled exception occurred: {Message}\", exception.Message);\n\n        var (status, title) = exception switch\n        {\n            KeyNotFoundException => (StatusCodes.Status404NotFound, \"Resource Not Found\"),\n            UnauthorizedAccessException => (StatusCodes.Status401Unauthorized, \"Unauthorized\"),\n            ArgumentException => (StatusCodes.Status400BadRequest, \"Invalid Argument\"),\n            _ => (StatusCodes.Status500InternalServerError, \"An unexpected server error occurred\")\n        };\n\n        var problemDetails = new ProblemDetails\n        {\n            Status = status,\n            Title = title,\n            Detail = exception.Message,\n            Instance = httpContext.Request.Path\n        };\n\n        httpContext.Response.StatusCode = status;\n        await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);\n\n        // Return true to indicate the exception has been completely handled\n        return true;\n    }\n}\n\n// 2. Register in Program.cs\nvar builder = WebApplication.CreateBuilder(args);\nbuilder.Services.AddExceptionHandler<GlobalExceptionHandler>();\nbuilder.Services.AddProblemDetails(); // Adds ProblemDetails services\n\nvar app = builder.Build();\n\n// Must be placed at the very top of the pipeline!\napp.UseExceptionHandler();\n\napp.MapGet(\"/crash\", () => { throw new KeyNotFoundException(\"Customer ID 42 was not found in database.\"); });\n\napp.Run();",
    "tip": "Always register `app.UseExceptionHandler()` as the very first middleware in `Program.cs` so it can catch exceptions thrown by all subsequent middlewares, controllers, and endpoints.",
    "summary": ".NET 8 IExceptionHandler provides a clean, standardized, high-performance way to catch all unhandled exceptions and return RFC 7807 ProblemDetails."
  },
  {
    "id": 29,
    "title": "29. JWT Authentication & Refresh Token Rotation",
    "category": "Phase 6: Security & Authentication",
    "language": "csharp",
    "explanation": "<h3>Production Token-Based Security Architecture</h3>\n<p>In modern web applications, stateless <strong>JSON Web Tokens (JWT)</strong> are the standard for securing REST APIs. However, storing short-lived Access Tokens in browser <code>localStorage</code> leaves them vulnerable to Cross-Site Scripting (XSS).</p>\n<h4>Production Security Best Practices:</h4>\n<ol>\n    <li><strong>Access Token (Short-Lived: 15 minutes)</strong>: Transmitted in the <code>Authorization: Bearer <token></code> header. Used to authenticate API requests.</li>\n    <li><strong>Refresh Token (Long-Lived: 7 days)</strong>: Stored in the database and sent to the client inside a secure <strong><code>HttpOnly, Secure, SameSite=Strict</code> Cookie</strong> (inaccessible to JavaScript, immune to XSS).</li>\n    <li><strong>Token Rotation</strong>: Every time a refresh token is used, it is immediately invalidated and replaced with a new one. If a stolen token is reused, the entire token family is revoked (Breach Detection).</li>\n</ol>",
    "diagram": "flowchart TD\n    Client[\"Client App\"] -->|\"1. POST /api/auth/login\"| Server[\"ASP.NET Core Auth API\"]\n    Server -->|\"2. Validates credentials & Generates Tokens\"| DB[(\"Database\")]\n    Server -->|\"3. Returns AccessToken (JSON) + RefreshToken (HttpOnly Cookie)\"| Client\n    Client -->|\"4. Calls Protected API with Bearer Token\"| Server\n    Client -->|\"5. When 401 Unauthorized -> Calls /api/auth/refresh\"| Server\n    Server -->|\"6. Validates & Rotates Refresh Token\"| Client\n    \n    style Client fill:#3b82f6,color:#fff\n    style Server fill:#8b5cf6,color:#fff\n    style DB fill:#10b981,color:#fff",
    "code": "// 1. Configure JWT Bearer in Program.cs\nusing Microsoft.AspNetCore.Authentication.JwtBearer;\nusing Microsoft.IdentityModel.Tokens;\nusing System.Text;\n\nvar builder = WebApplication.CreateBuilder(args);\nvar jwtKey = builder.Configuration[\"Jwt:Key\"] ?? \"SUPER_SECRET_KEY_AT_LEAST_32_BYTES_LONG!\";\n\nbuilder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)\n    .AddJwtBearer(options =>\n    {\n        options.TokenValidationParameters = new TokenValidationParameters\n        {\n            ValidateIssuer = true,\n            ValidateAudience = true,\n            ValidateLifetime = true,\n            ValidateIssuerSigningKey = true,\n            ValidIssuer = builder.Configuration[\"Jwt:Issuer\"],\n            ValidAudience = builder.Configuration[\"Jwt:Audience\"],\n            IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(jwtKey)),\n            ClockSkew = TimeSpan.Zero // Strict expiration without 5-minute leeway\n        };\n    });\n\nbuilder.Services.AddAuthorization();\nvar app = builder.Build();\n\napp.UseAuthentication();\napp.UseAuthorization();\n\n// 2. Token Generation Service\npublic class TokenService\n{\n    public string GenerateAccessToken(User user)\n    {\n        var claims = new[]\n        {\n            new Claim(ClaimTypes.NameIdentifier, user.Id.ToString()),\n            new Claim(ClaimTypes.Email, user.Email),\n            new Claim(ClaimTypes.Role, user.Role)\n        };\n\n        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(\"SUPER_SECRET_KEY_AT_LEAST_32_BYTES_LONG!\"));\n        var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);\n\n        var token = new JwtSecurityToken(\n            issuer: \"vinnertech.com\",\n            audience: \"vinnertech.com\",\n            claims: claims,\n            expires: DateTime.UtcNow.AddMinutes(15),\n            signingCredentials: creds\n        );\n\n        return new JwtSecurityTokenHandler().WriteToken(token);\n    }\n}",
    "summary": "JWT access tokens provide stateless authentication, while secure HttpOnly refresh token rotation prevents XSS vulnerabilities and ensures seamless user sessions."
  },
  {
    "id": 30,
    "title": "30. Distributed Caching with Redis & Cache Aside Pattern",
    "category": "Phase 7: Performance & Caching",
    "language": "csharp",
    "explanation": "<h3>Scaling Read Performance Across Multiple Server Instances</h3>\n<p>When an application scales across multiple servers or Docker containers (e.g. in Kubernetes), <strong>In-Memory Cache (<code>IMemoryCache</code>)</strong> is no longer sufficient because each server instance has its own isolated memory, leading to inconsistent data.</p>\n<p><strong>Distributed Caching with Redis</strong> stores cached data in an external, in-memory key-value database shared across all server instances.</p>\n<h4>The Cache-Aside Pattern:</h4>\n<ol>\n    <li>Application checks Redis cache for the requested key.</li>\n    <li><strong>Cache Hit</strong>: Return data immediately (sub-millisecond latency).</li>\n    <li><strong>Cache Miss</strong>: Fetch data from SQL Server, store it in Redis with an expiration time, and return it to the caller.</li>\n</ol>",
    "diagram": "flowchart LR\n    App[\"ASP.NET Core App Instance 1 & 2\"] -->|\"1. Check Cache (GET user:42)\"| Redis[(\"Redis Cache Cluster\")]\n    Redis -.->|\"Cache Hit (Return Data)\"| App\n    Redis -- \"Cache Miss\" --> SQL[(\"SQL Server Database\")]\n    SQL -->|\"Fetch Record\"| App\n    App -->|\"2. Populate Cache (SET user:42 EX 300)\"| Redis\n    \n    style App fill:#3b82f6,color:#fff\n    style Redis fill:#dc2626,color:#fff\n    style SQL fill:#10b981,color:#fff",
    "code": "// 1. Install Package: Microsoft.Extensions.Caching.StackExchangeRedis\n// In Program.cs:\nbuilder.Services.AddStackExchangeRedisCache(options =>\n{\n    options.Configuration = builder.Configuration.GetConnectionString(\"Redis\") ?? \"localhost:6379\";\n    options.InstanceName = \"VinnerTech_\";\n});\n\n// 2. Generic Cache-Aside Service Extension\npublic static class DistributedCacheExtensions\n{\n    public static async Task<T?> GetOrSetAsync<T>(\n        this IDistributedCache cache,\n        string key,\n        Func<Task<T>> factory,\n        TimeSpan? absoluteExpiration = null,\n        CancellationToken ct = default)\n    {\n        var cachedBytes = await cache.GetAsync(key, ct);\n        if (cachedBytes is not null)\n        {\n            var json = Encoding.UTF8.GetString(cachedBytes);\n            return JsonSerializer.Deserialize<T>(json);\n        }\n\n        // Cache Miss: execute database query\n        var data = await factory();\n        if (data is not null)\n        {\n            var options = new DistributedCacheEntryOptions\n            {\n                AbsoluteExpirationRelativeToNow = absoluteExpiration ?? TimeSpan.FromMinutes(10),\n                SlidingExpiration = TimeSpan.FromMinutes(2)\n            };\n            var serialized = JsonSerializer.Serialize(data);\n            await cache.SetAsync(key, Encoding.UTF8.GetBytes(serialized), options, ct);\n        }\n\n        return data;\n    }\n}",
    "summary": "Distributed caching with Redis ensures consistent, blazing-fast read access across horizontally scaled web application nodes."
  },
  {
    "id": 31,
    "title": "31. Built-in Rate Limiting Middleware (.NET 8+)",
    "category": "Phase 8: Security & Traffic Management",
    "language": "csharp",
    "explanation": "<h3>Protecting APIs from Abuse, DDoS, and Scraping</h3>\n<p>.NET 7 and .NET 8 include a production-grade, built-in <strong>Rate Limiting Middleware</strong> in <code>Microsoft.AspNetCore.RateLimiting</code>, removing the need for third-party libraries like AspNetCoreRateLimit.</p>\n<h4>Supported Rate Limiting Algorithms:</h4>\n<ul>\n    <li><strong>Fixed Window</strong>: Allows X requests within a fixed time window (e.g. 100 requests per 1 minute).</li>\n    <li><strong>Sliding Window</strong>: Divides the window into segments for smoother traffic distribution.</li>\n    <li><strong>Token Bucket</strong>: Tokens accumulate at a constant rate; requests consume tokens. Supports bursts.</li>\n    <li><strong>Concurrency Limiter</strong>: Restricts concurrent active requests to prevent resource starvation.</li>\n</ul>",
    "diagram": "flowchart TD\n    Req[\"Incoming Requests\"] --> RateLimit[\"ASP.NET Core RateLimiter Middleware\"]\n    RateLimit -->|\"Under 100 req/min\"| API[\"Execute API Endpoint (200 OK)\"]\n    RateLimit -->|\"Exceeded Limit\"| Reject[\"Return HTTP 429 Too Many Requests\"]\n    \n    style Req fill:#3b82f6,color:#fff\n    style RateLimit fill:#8b5cf6,color:#fff\n    style API fill:#10b981,color:#fff\n    style Reject fill:#ef4444,color:#fff",
    "code": "// In Program.cs:\nusing Microsoft.AspNetCore.RateLimiting;\nusing System.Threading.RateLimiting;\n\nvar builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddRateLimiter(options =>\n{\n    options.RejectionStatusCode = StatusCodes.Status429TooManyRequests;\n\n    // Policy 1: Global Fixed Window (100 req per minute per IP)\n    options.AddPolicy(\"fixed-ip\", httpContext =>\n        RateLimitPartition.GetFixedWindowLimiter(\n            partitionKey: httpContext.Connection.RemoteIpAddress?.ToString() ?? \"anonymous\",\n            factory: partition => new FixedWindowRateLimiterOptions\n            {\n                PermitLimit = 100,\n                Window = TimeSpan.FromMinutes(1),\n                QueueProcessingOrder = QueueProcessingOrder.OldestFirst,\n                QueueLimit = 5\n            }));\n\n    // Policy 2: Token Bucket for payment endpoints\n    options.AddTokenBucketLimiter(\"payment-policy\", opt =>\n    {\n        opt.TokenLimit = 10;\n        opt.ReplenishmentPeriod = TimeSpan.FromSeconds(10);\n        opt.TokensPerPeriod = 2;\n        opt.AutoReplenishment = true;\n    });\n});\n\nvar app = builder.Build();\n\napp.UseRateLimiter();\n\n// Apply policy to specific endpoints\napp.MapGet(\"/api/data\", () => Results.Ok(\"Data fetched successfully!\"))\n   .RequireRateLimiting(\"fixed-ip\");\n\napp.MapPost(\"/api/checkout\", () => Results.Ok(\"Payment processed!\"))\n   .RequireRateLimiting(\"payment-policy\");\n\napp.Run();",
    "summary": ".NET 8 built-in Rate Limiter protects endpoints against DDoS and brute-force attacks using Fixed Window, Sliding Window, and Token Bucket algorithms."
  },
  {
    "id": 32,
    "title": "32. Background Tasks & Channels (IHostedService & BackgroundService)",
    "category": "Phase 9: Asynchronous Processing & Background Jobs",
    "language": "csharp",
    "explanation": "<h3>Executing Long-Running Jobs Without Blocking HTTP Requests</h3>\n<p>Operations such as sending welcome emails, generating PDF reports, or processing payment webhooks should never block the user's HTTP request.</p>\n<p>ASP.NET Core provides <strong><code>BackgroundService</code></strong> (which implements <code>IHostedService</code>) to run long-running background tasks in a separate thread managed by the application host.</p>\n<p>Combined with <strong><code>System.Threading.Channels</code></strong>, you can create a high-performance in-memory producer-consumer queue without external dependencies.</p>",
    "diagram": "flowchart LR\n    API[\"Controller / Endpoint\"] -->|\"1. Enqueue Job (Non-blocking)\"| Channel[(\"Bounded Channel Queue\")]\n    API -->|\"2. Return HTTP 202 Accepted\"| User[\"User Browser\"]\n    Channel -->|\"3. Read Background Job\"| Worker[\"BackgroundService Worker\"]\n    Worker -->|\"4. Process Job & Send Email\"| SMTP[\"Email / Report Service\"]\n    \n    style API fill:#3b82f6,color:#fff\n    style Channel fill:#f59e0b,color:#fff\n    style Worker fill:#8b5cf6,color:#fff\n    style SMTP fill:#10b981,color:#fff",
    "code": "// 1. Create a Thread-Safe In-Memory Queue using System.Threading.Channels\npublic class BackgroundTaskQueue\n{\n    private readonly Channel<Func<IServiceProvider, CancellationToken, ValueTask>> _queue;\n\n    public BackgroundTaskQueue(int capacity = 100)\n    {\n        var options = new BoundedChannelOptions(capacity) { FullMode = BoundedChannelFullMode.Wait };\n        _queue = Channel.CreateBounded<Func<IServiceProvider, CancellationToken, ValueTask>>(options);\n    }\n\n    public async ValueTask QueueBackgroundWorkItemAsync(Func<IServiceProvider, CancellationToken, ValueTask> workItem)\n    {\n        ArgumentNullException.ThrowIfNull(workItem);\n        await _queue.Writer.WriteAsync(workItem);\n    }\n\n    public async ValueTask<Func<IServiceProvider, CancellationToken, ValueTask>> DequeueAsync(CancellationToken ct)\n    {\n        return await _queue.Reader.ReadAsync(ct);\n    }\n}\n\n// 2. The Background Worker\npublic class QueuedHostedService : BackgroundService\n{\n    private readonly BackgroundTaskQueue _taskQueue;\n    private readonly IServiceProvider _serviceProvider;\n    private readonly ILogger<QueuedHostedService> _logger;\n\n    public QueuedHostedService(BackgroundTaskQueue taskQueue, IServiceProvider serviceProvider, ILogger<QueuedHostedService> logger)\n    {\n        _taskQueue = taskQueue;\n        _serviceProvider = serviceProvider;\n        _logger = logger;\n    }\n\n    protected override async Task ExecuteAsync(CancellationToken stoppingToken)\n    {\n        _logger.LogInformation(\"Queued Hosted Service is running.\");\n\n        while (!stoppingToken.IsCancellationRequested)\n        {\n            var workItem = await _taskQueue.DequeueAsync(stoppingToken);\n\n            try\n            {\n                using var scope = _serviceProvider.CreateScope();\n                await workItem(scope.ServiceProvider, stoppingToken);\n            }\n            catch (Exception ex)\n            {\n                _logger.LogError(ex, \"Error occurred executing work item.\");\n            }\n        }\n    }\n}",
    "summary": "BackgroundService and System.Threading.Channels enable clean, asynchronous background processing for email, reports, and queued tasks without blocking HTTP requests."
  },
  {
    "id": 33,
    "title": "33. Production Multi-Stage Dockerfile & Containerization",
    "category": "Phase 10: DevOps & Cloud Deployment",
    "language": "dockerfile",
    "explanation": "<h3>Packaging ASP.NET Core for Kubernetes and Cloud Deployment</h3>\n<p>In enterprise deployments, ASP.NET Core applications are containerized using Docker. To achieve minimal container image size (under 120MB) and maximum security, we use <strong>Multi-Stage Docker Builds</strong>.</p>\n<h4>Multi-Stage Principles:</h4>\n<ol>\n    <li><strong>Build Stage (SDK)</strong>: Uses the heavy <code>mcr.microsoft.com/dotnet/sdk:8.0</code> image containing compilers to restore NuGet packages and build the <code>.dll</code> binaries.</li>\n    <li><strong>Publish Stage</strong>: Compiles and optimizes the release bundle with <code>dotnet publish -c Release</code>.</li>\n    <li><strong>Runtime Stage (ASP.NET)</strong>: Uses the lean <code>mcr.microsoft.com/dotnet/aspnet:8.0-alpine</code> (or chiseled Ubuntu) containing ONLY the runtime engine without compilers or root privileges.</li>\n</ol>",
    "diagram": "flowchart TD\n    SDK[\"Stage 1: SDK Container (750MB)\nRestore & Compile C# Code\"] --> Publish[\"Stage 2: Publish Output (.dll files)\"]\n    Publish --> Final[\"Stage 3: Alpine Runtime (110MB)\nCopies ONLY binaries\nRuns as Non-Root User\"]\n    Final --> Deploy[\"Production Kubernetes / AWS ECS / Azure App Service\"]\n    \n    style SDK fill:#f59e0b,color:#fff\n    style Final fill:#10b981,color:#fff\n    style Deploy fill:#3b82f6,color:#fff",
    "code": "# Multi-Stage Dockerfile for ASP.NET Core 8\n# STAGE 1: Base Runtime Environment\nFROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine AS base\nWORKDIR /app\nEXPOSE 8080\nEXPOSE 8081\n# Run as non-root user for enterprise security\nUSER $APP_UID\n\n# STAGE 2: Build Application using SDK\nFROM mcr.microsoft.com/dotnet/sdk:8.0-alpine AS build\nARG BUILD_CONFIGURATION=Release\nWORKDIR /src\n\n# Copy csproj and restore dependencies (Cached layer)\nCOPY [\"VinnerTech.Api/VinnerTech.Api.csproj\", \"VinnerTech.Api/\"]\nRUN dotnet restore \"VinnerTech.Api/VinnerTech.Api.csproj\"\n\n# Copy full source code and build\nCOPY . .\nWORKDIR \"/src/VinnerTech.Api\"\nRUN dotnet build \"VinnerTech.Api.csproj\" -c $BUILD_CONFIGURATION -o /app/build\n\n# STAGE 3: Publish Release Artifacts\nFROM build AS publish\nARG BUILD_CONFIGURATION=Release\nRUN dotnet publish \"VinnerTech.Api.csproj\" -c $BUILD_CONFIGURATION -o /app/publish /p:UseAppHost=false\n\n# STAGE 4: Final Lean Production Image\nFROM base AS final\nWORKDIR /app\nCOPY --from=publish /app/publish .\nENTRYPOINT [\"dotnet\", \"VinnerTech.Api.dll\"]",
    "tip": "Always copy the `.csproj` file and run `dotnet restore` before copying the rest of your source code. This allows Docker to cache your NuGet package downloads, reducing build times from minutes to seconds.",
    "summary": "Multi-stage Docker builds produce lightweight, secure, non-root container images optimized for cloud and Kubernetes deployments."
  },
  {
    "id": 34,
    "title": "Master Project: Enterprise ASP.NET Core REST API Architecture",
    "category": "Phase 11: End-to-End Practical Project",
    "language": "csharp",
    "explanation": "### Master Project: Full-Scale Production REST Web API\n\nBuild an enterprise-grade ASP.NET Core Web API with **Entity Framework Core, JWT Authentication, Global Exception Handling with ProblemDetails, Repository Pattern, and Swagger OpenAPI** in step-by-step baby steps.\n\n```text\nEcommerceApi/\n├── Controllers/\n│   ├── AuthController.cs            <- POST /api/auth/login, POST /api/auth/register\n│   └── ProductsController.cs        <- GET, POST, PUT, DELETE /api/products (Secure)\n├── Data/\n│   └── AppDbContext.cs              <- EF Core DbContext & Entity Configuration\n├── Entities/\n│   ├── User.cs                      <- User Entity with Password Hash & Roles\n│   └── Product.cs                   <- Product Entity\n├── DTOs/\n│   ├── AuthDtos.cs                  <- LoginRequest, AuthResponse, RegisterRequest\n│   └── ProductDtos.cs               <- CreateProductDto, ProductResponseDto\n├── Middleware/\n│   └── GlobalExceptionHandler.cs    <- RFC 7807 ProblemDetails Exception Handler\n├── Services/\n│   └── ITokenService.cs             <- JWT Token Generator with Claims\n├── appsettings.json                 <- ConnectionStrings & JWT Key configuration\n└── Program.cs                       <- DI Setup, Authentication & Middleware Pipeline\n```\n\n### Complete 7 Baby Steps to Build the Production API:\n- **Step 1: Project Scaffolding**: Create project and install EF Core, JWT Bearer, and Swagger packages.\n- **Step 2: Database Entities & DbContext**: Configure SQL Server tables with Entity Framework.\n- **Step 3: JWT Security & Token Service**: Issue signed tokens with user roles and claims.\n- **Step 4: Centralized Error Handler**: Return RFC 7807 `ProblemDetails` for clean client error consumption.\n- **Step 5: Secure REST Controllers**: Implement full CRUD with `[Authorize]` attributes and `ActionResult<T>`.\n- **Step 6: Program.cs Configuration**: Wire up DI lifetimes, middleware pipeline, and Swagger JWT Authorization.\n- **Step 7: Verification**: Test all endpoints using Swagger UI and curl.",
    "diagram": "flowchart TD\n    Client[\"Client / Swagger UI\"] -->|\"1. POST /api/auth/login\"| Auth[\"AuthController (Validates & Issues JWT)\"]\n    Client -->|\"2. GET /api/products with Bearer Token\"| AuthMiddleware[\"JWT Auth Middleware\"]\n    AuthMiddleware -->|\"3. Authorized\"| Controller[\"ProductsController\"]\n    Controller -->|\"4. LINQ Query\"| EF[\"Entity Framework Core (AppDbContext)\"]\n    EF --> DB[(\"SQL Server / PostgreSQL\")]\n    \n    style Client fill:#3b82f6,color:#fff\n    style AuthMiddleware fill:#8b5cf6,color:#fff\n    style Controller fill:#10b981,color:#fff\n    style DB fill:#f59e0b,color:#fff",
    "code": "// STEP 1: Dependencies (Run via Terminal CLI)\n// dotnet new webapi -n EcommerceApi\n// cd EcommerceApi\n// dotnet add package Microsoft.EntityFrameworkCore.SqlServer\n// dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer\n// dotnet add package Swashbuckle.AspNetCore\n\n// STEP 2: Database Context (Data/AppDbContext.cs)\nusing Microsoft.EntityFrameworkCore;\n\npublic class Product\n{\n    public int Id { get; set; }\n    public string Name { get; set; } = string.Empty;\n    public string Category { get; set; } = string.Empty;\n    public decimal Price { get; set; }\n    public int Stock { get; set; }\n}\n\npublic class AppDbContext : DbContext\n{\n    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options) {}\n    public DbSet<Product> Products => Set<Product>();\n}\n\n// STEP 3: DTOs & Validation (DTOs/ProductDtos.cs)\npublic record CreateProductDto(\n    [Required, MinLength(2)] string Name,\n    [Required] string Category,\n    [Range(0.01, 100000)] decimal Price,\n    [Range(0, 10000)] int Stock\n);\n\npublic record ProductResponseDto(int Id, string Name, string Category, decimal Price, int Stock);\n\n// STEP 4: REST Controller (Controllers/ProductsController.cs)\nusing Microsoft.AspNetCore.Authorization;\nusing Microsoft.AspNetCore.Mvc;\nusing Microsoft.EntityFrameworkCore;\n\n[ApiController]\n[Route(\"api/[controller]\")]\npublic class ProductsController : ControllerBase\n{\n    private readonly AppDbContext _db;\n\n    public ProductsController(AppDbContext db)\n    {\n        _db = db;\n    }\n\n    [HttpGet]\n    public async Task<ActionResult<IEnumerable<ProductResponseDto>>> GetAll(CancellationToken ct)\n    {\n        var products = await _db.Products\n            .AsNoTracking()\n            .Select(p => new ProductResponseDto(p.Id, p.Name, p.Category, p.Price, p.Stock))\n            .ToListAsync(ct);\n\n        return Ok(products);\n    }\n\n    [HttpGet(\"{id:int}\")]\n    public async Task<ActionResult<ProductResponseDto>> GetById(int id, CancellationToken ct)\n    {\n        var p = await _db.Products.FindAsync(new object[] { id }, ct);\n        if (p == null) return NotFound(new { Message = $\"Product with ID {id} not found.\" });\n\n        return Ok(new ProductResponseDto(p.Id, p.Name, p.Category, p.Price, p.Stock));\n    }\n\n    [HttpPost]\n    [Authorize(Roles = \"Admin\")]\n    public async Task<ActionResult<ProductResponseDto>> Create([FromBody] CreateProductDto dto, CancellationToken ct)\n    {\n        var product = new Product\n        {\n            Name = dto.Name,\n            Category = dto.Category,\n            Price = dto.Price,\n            Stock = dto.Stock\n        };\n\n        _db.Products.Add(product);\n        await _db.SaveChangesAsync(ct);\n\n        var response = new ProductResponseDto(product.Id, product.Name, product.Category, product.Price, product.Stock);\n        return CreatedAtAction(nameof(GetById), new { id = product.Id }, response);\n    }\n\n    [HttpDelete(\"{id:int}\")]\n    [Authorize(Roles = \"Admin\")]\n    public async Task<IActionResult> Delete(int id, CancellationToken ct)\n    {\n        var p = await _db.Products.FindAsync(new object[] { id }, ct);\n        if (p == null) return NotFound();\n\n        _db.Products.Remove(p);\n        await _db.SaveChangesAsync(ct);\n        return NoContent();\n    }\n}\n\n// STEP 5: Program.cs Configuration\nvar builder = WebApplication.CreateBuilder(args);\n\n// Register DbContext with In-Memory / SQL Server\nbuilder.Services.AddDbContext<AppDbContext>(opt => \n    opt.UseInMemoryDatabase(\"EcommerceDb\"));\n\n// Register JWT Authentication\nbuilder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)\n    .AddJwtBearer(options =>\n    {\n        options.TokenValidationParameters = new TokenValidationParameters\n        {\n            ValidateIssuer = true,\n            ValidateAudience = true,\n            ValidateLifetime = true,\n            ValidateIssuerSigningKey = true,\n            ValidIssuer = \"ecommerce.com\",\n            ValidAudience = \"ecommerce.com\",\n            IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(\"SUPER_SECRET_PRODUCTION_KEY_AT_LEAST_32_BYTES!\"))\n        };\n    });\n\nbuilder.Services.AddAuthorization();\nbuilder.Services.AddControllers();\nbuilder.Services.AddEndpointsApiExplorer();\nbuilder.Services.AddSwaggerGen();\n\nvar app = builder.Build();\n\nif (app.Environment.IsDevelopment())\n{\n    app.UseSwagger();\n    app.UseSwaggerUI();\n}\n\napp.UseHttpsRedirection();\napp.UseAuthentication();\napp.UseAuthorization();\napp.MapControllers();\n\napp.Run();",
    "tip": "Always use `AsNoTracking()` for read-only queries (`GET`) in Entity Framework Core. This disables EF change tracking and reduces memory usage by over 40%.",
    "summary": "A complete, production-ready ASP.NET Core REST API featuring Entity Framework Core, JWT authentication, DTOs, and Swagger documentation."
  }
];
